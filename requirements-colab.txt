# Colab already provides a CUDA-compatible PyTorch build. Reinstalling a
# separately pinned torch wheel can silently replace it with a CPU-only or
# CUDA-incompatible build, so torch is intentionally not listed here.
numpy>=1.26,<3
pandas>=2.0,<4
scikit-learn>=1.4,<2
PyYAML>=6,<7
pytest>=8,<10
